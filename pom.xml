<project xmlns="http://maven.apache.org/POM/4.0.0"   
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  
  <groupId>io.nmoncho</groupId>
  <artifactId>faradn</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <dependencies>
    <dependency>
      <groupId>org.usb4java</groupId>
      <artifactId>usb4java-javax</artifactId>
      <version>${usb4java.version}</version>
    </dependency>

    <dependency>
      <groupId>com.typesafe</groupId>
      <artifactId>config</artifactId>
      <version>${typesafeconfig.version}</version>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>

    <dependency>
      <groupId>org.jsoup</groupId>
      <artifactId>jsoup</artifactId>
      <version>${jsoup.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <version>${logback.version}</version>
      <scope>test</scope>
    </dependency>
    
    <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${mockito.version}</version>
        <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
        <plugin>
          <groupId>net.revelc.code.formatter</groupId>
          <artifactId>formatter-maven-plugin</artifactId>
          <version>${formatter.version}</version>
          <executions>
            <execution>
              <goals>
                <goal>format</goal>
              </goals>
            </execution>
          </executions>
          <configuration>
            <encoding>UTF-8</encoding>
            <configFile>${project.basedir}/eclipse-formatter.xml</configFile>
          </configuration>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.version}</version>
          <configuration>
            <argLine>-XX:+EnableDynamicAgentLoading</argLine>
          </configuration>
        </plugin>
        <plugin>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${surefire.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>${enforcer.version}</version>
          <executions>
            <execution>
              <id>enforce</id>
              <goals>
                <goal>enforce</goal>
              </goals>
              <configuration>
                <rules>
                  <requireJavaVersion>
                    <version>[17,)</version>
                  </requireJavaVersion>
                  <requireMavenVersion>
                    <version>[3.8,)</version>
                  </requireMavenVersion>
                  <dependencyConvergence/>
                </rules>
              </configuration>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </build>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.release>${java.version}</maven.compiler.release>
    <formatter.version>2.23.0</formatter.version>
    <enforcer.version>3.4.1</enforcer.version>
    <surefire.version>3.2.5</surefire.version>
    <usb4java.version>1.3.0</usb4java.version>
    <typesafeconfig.version>1.4.3</typesafeconfig.version>
    <slf4j.version>2.0.13</slf4j.version>
    <jsoup.version>1.17.2</jsoup.version>
    <junit.version>5.10.2</junit.version>
    <logback.version>1.5.6</logback.version>
    <mockito.version>5.11.0</mockito.version>
  </properties>

  <profiles>
    <!--
      usb4java 1.3.0 bundles libusb4java natives for darwin-x86-64,
      linux-x86-64, linux-aarch64 and Windows, but NOT darwin-aarch64
      (Apple Silicon). On that platform we add the native explicitly. It is
      not published to Maven Central, so it must be built and installed into
      the local repository once:

        mvn install:install-file -Dfile=libusb4java-1.3.0-darwin-aarch64.jar \
          -DgroupId=org.usb4java -DartifactId=libusb4java -Dversion=1.3.0 \
          -Dclassifier=darwin-aarch64 -Dpackaging=jar

      On every other OS/arch this profile stays inactive and usb4java resolves
      the correct native transitively from Maven Central.
    -->
    <profile>
      <id>macos-aarch64</id>
      <activation>
        <os>
          <family>mac</family>
          <arch>aarch64</arch>
        </os>
      </activation>
      <dependencies>
        <dependency>
          <groupId>org.usb4java</groupId>
          <artifactId>libusb4java</artifactId>
          <version>${usb4java.version}</version>
          <classifier>darwin-aarch64</classifier>
        </dependency>
      </dependencies>
    </profile>
  </profiles>


</project>
